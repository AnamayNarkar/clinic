version: "2"
sql:
  - schema: "./sql/schema/clinic_dump.sql"
    queries: "./sql/queries"
    engine: "postgresql"
    gen:
      go:
        out: "sqlc"
        emit_json_tags: true
        # initialisms: []
